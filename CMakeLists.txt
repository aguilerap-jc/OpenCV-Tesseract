CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

PROJECT(CMakeTessOpenCV1)

FIND_PACKAGE(OpenCV REQUIRED)

ADD_EXECUTABLE(CMakeTessOpenCV1 main.cpp)

TARGET_LINK_LIBRARIES(CMakeTessOpenCV1 ${OpenCV_LIBS} lept tesseract)
